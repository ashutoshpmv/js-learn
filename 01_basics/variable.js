// const used for constants and non-changing values.
const a=123

//let is used for values that can be changed with time.
let myName="Ashu"

/*
1. var is not used now. earlier it was used.
2. semicolon can/cant be used, no effect.
*/

console.log(a);

// only Declaring a variable then it holds "undefined" value
let accName;

console.table([a,myName,accName])
